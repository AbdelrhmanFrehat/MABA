<UserControl x:Class="MabaControlCenter.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundDarkBrush}">
    <StackPanel Margin="24" VerticalAlignment="Stretch">
        <TextBlock Text="{Binding Source={StaticResource Labels}, Path=Page_Logs_Title}" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"/>
        <TextBlock Text="{Binding Source={StaticResource Labels}, Path=Page_Logs_Placeholder}" FontSize="14"
                   Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="{Binding Source={StaticResource Labels}, Path=Button_ClearLogs}" Command="{Binding ClearLogsCommand}"
                    Padding="12,8" Margin="0,0,8,0"
                    Background="{DynamicResource BackgroundLightBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"/>
            <Button Content="{Binding Source={StaticResource Labels}, Path=Button_ExportToTxt}" Command="{Binding ExportLogsCommand}"
                    Padding="12,8"
                    Background="{DynamicResource BackgroundLightBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"/>
        </StackPanel>

        <Border Background="{DynamicResource BackgroundLightBrush}" BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1" Padding="8" MinHeight="300">
            <DataGrid ItemsSource="{Binding Logs}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Background="Transparent" RowBackground="Transparent"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      FontFamily="Consolas" FontSize="13"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                      RowHeight="28"
                      CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource BackgroundMediumBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        <Setter Property="Padding" Value="8,6"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                <Setter Property="Background" Value="{DynamicResource ErrorHighlightBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{Binding Source={StaticResource Labels}, Path=Logs_Timestamp}" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="160"/>
                    <DataGridTextColumn Header="{Binding Source={StaticResource Labels}, Path=Logs_Direction}" Binding="{Binding Direction}" Width="80"/>
                    <DataGridTextColumn Header="{Binding Source={StaticResource Labels}, Path=Logs_Message}" Binding="{Binding Message}" Width="*"/>
                    <DataGridTextColumn Header="{Binding Source={StaticResource Labels}, Path=Logs_Status}" Binding="{Binding Status}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </StackPanel>
</UserControl>
