try { console.log('main', typeof require('electron/main').app); } catch (e) { console.log('main-error', e.message); } try { const common = require('electron/common'); console.log('common', typeof common.app, typeof common.BrowserWindow); } catch (e) { console.log('common-error', e.message); } try { const cross = require('electron'); console.log('electron', typeof cross, Object.keys(cross).slice(0,5)); } catch (e) { console.log('electron-error', e.message); }
